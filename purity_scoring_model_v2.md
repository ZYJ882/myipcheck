# MyIPCheck 透明纯净度评分模型 v2.1

**版本：** 2.1（细粒度连续映射）
**状态：** 独立、可解释的公开网络出口风险信号指数；不是任意第三方服务的原始算法，也不是“欺诈概率”。

## 1. 分数的含义与边界

MyIPCheck 的总分是 `0.0–100.0` 的**当前公网出口公开风险信号指数**。100.0 表示本次成功覆盖的公开来源中未见可计入风险的信号，0.0 表示多个强风险桶同时接近上限。它不判断个人、账号、支付、交易或业务是否欺诈，也不能把“未检测到”解释为安全保证。

> 有真实结果标签的数据才能检验分数与实际事件率是否校准。没有此类标签时，模型应输出可解释的提示 / 排序指数，而不能宣称精确风险概率。[1]

v2.1 的核心变化是：**有 0–100 原始输入的证据不再按 25 / 50 / 75 等固定区间跳档**。AbuseIPDB 原始分、报告数量、报告时效、ProxyCheck 风险、ProxyCheck 置信度、ProxyCheck 最后检出时间以及攻击历史都会进入连续公式。只有 API 天生仅返回“是 / 否”的字段，例如 Tor Project 的官方确认、ipapi.is 的 `is_abuser`，才保留明确的二元证据权重；APP 会在明细中说明这一限制，而不会伪造不存在的精度。[2] [3]

## 2. 总公式、类别上限与展示精度

先在四个风险桶内计算原始风险并执行封顶，再求和：

```text
总风险 = clamp(匿名化 + 滥用攻击 + 基础设施 + 观测完整性, 0, 100)
总分   = 100 - 总风险
```

APP 内部使用 `Double` 计算，并在首页显示一位小数。因此 AbuseIPDB 从 30 改为 31、同一 ProxyCheck 风险从 40% 但置信度 / 时效不同，都会产生可见的分数变化。

| 风险桶 | 上限 | 允许进入的信号 | 误报控制 |
| --- | ---: | --- | --- |
| 匿名化网络 | 45.0 | Tor、代理、VPN | 同类只取最强结论；双来源只给予有限支持增量。 |
| 滥用与攻击 | 40.0 | AbuseIPDB、ProxyCheck 风险、受损、滥用、爬虫、攻击历史 | 同类风险不线性叠加；独立来源支持增量最高 4.0。 |
| 托管基础设施 | 12.0 | ASN / 组织启发式、托管、数据中心 | 托管不等同恶意，严格限制为轻量提示。 |
| 观测完整性 | 8.0 | 两个出口 IP 来源冲突 | 国家库与 IPv4 / IPv6 地理差异只展示，不计风险。 |

## 3. 公共连续缩放函数

### 3.1 时效系数

当来源返回 ISO 8601 的“最近发现 / 最近报告”时间时，先计算距今天数 `d`，随后使用：

```text
F(d) = clamp(0.45 + 0.55 × e^(-d / 21), 0.45, 1.00)
```

这意味着新近信号接近 `1.00`，随时间平滑衰减，最长只保留 `0.45` 的权重；它不会在第 7、30 或 90 天突然跳变。时间缺失时，ProxyCheck 使用保守回退 `0.70`，AbuseIPDB 使用 `0.60`，而不是把缺失误当作最新或最旧。AbuseIPDB 的 Check 接口允许限制报告时间窗口，并返回 `lastReportedAt` 与窗口内 `totalReports`；ProxyCheck v3 返回 0–100 的 `confidence` 和 ISO 8601 `last_seen`。[2] [3]

### 3.2 ProxyCheck 可靠度系数

令 ProxyCheck 置信度为 `c`（0–100），最后发现时效为 `F`：

```text
R = clamp((0.30 + 0.70 × c / 100) × F, 0.25, 1.00)
```

`R` 是**供应商信号的可靠度系数**，不是“用户是代理的概率”。它只缩放 ProxyCheck 的代理、VPN、风险、受损、爬虫、攻击历史和托管信号；不缩放 Tor Project、AbuseIPDB 或 ipapi.is 的明确布尔结果。

## 4. 匿名化网络桶（上限 45.0）

| 证据情况 | 可复算风险 |
| --- | --- |
| Tor Project 官方确认 | `45.0` |
| 其他来源 Tor，且 ProxyCheck 也确认 | `38.0 + 4.0 × R` |
| 其他来源 Tor，但无 ProxyCheck Tor | `38.0` |
| 仅 ProxyCheck 代理 | `32.0 × R` |
| 仅 ipapi.is 代理 | `28.0` |
| 两者都确认代理 | `max(28.0, 32.0 × R) + 6.0 × R` |
| 仅 ProxyCheck VPN | `25.0 × R` |
| 仅 ipapi.is VPN | `22.0` |
| 两者都确认 VPN | `max(22.0, 25.0 × R) + 5.0 × R` |

最终取上表适用项中的最大值，再执行 `clamp(0, 45.0)`。Tor、代理和 VPN 是同一“匿名化网络”事实，不会彼此相加。Tor Project 是明确的官方二元检查，因此不应伪造小数置信度；该限制会直接呈现在 APP 的证据明细中。[4]

## 5. 滥用与攻击桶（上限 40.0）

### 5.1 AbuseIPDB 连续部分

令 `a` 为 `abuseConfidenceScore`（0–100），`n` 为 `totalReports`，`F_a` 为最近报告的时效系数：

```text
A_base    = 34.0 × (a / 100)^1.35
A_reports = 2.0 × ln(1 + n) / ln(101)
A_fresh   = 2.0 × F_a                  （仅 a > 0）
A          = min(36.0, A_base + A_reports + A_fresh)
```

幂函数 `1.35` 使低分报告只带来小提示、较高分增长更快，但仍然连续；对数函数使第 1、10、100 份报告逐步增加支持度，而不会把大量重复报告线性放大。AbuseIPDB 公开说明其 `abuseConfidenceScore` 是对报告所反映滥用情况的非二元评估，并提示低于 25 的范围不适合用作广泛处置门槛。[2]

### 5.2 ProxyCheck、攻击标记与独立支持

令 ProxyCheck 风险为 `p`（0–100），可靠度为 `R`，攻击历史总事件数为 `h`：

```text
P_vendor  = 16.0 × (p / 100)^1.20 × R
H_history = 4.0 × ln(1 + h) / ln(101) × R
```

明确布尔字段的可用原始信息只有“真 / 假”，因此其基准值是公开的模型权重，而非假装由供应商返回精确概率：

| 二元证据 | 风险计算 |
| --- | --- |
| ProxyCheck `compromised=true` | `12.0 + 8.0 × R` |
| ipapi.is `is_abuser=true` | `16.0` |
| 两者均为真 | 在下式中增加 `3.0` 的不同来源支持 |
| ProxyCheck `scraper=true` | `2.0 + 4.0 × R` |
| ipapi.is `is_crawler=true` | `4.0` |

先计算：

```text
D = min(28.0, max(受损风险, 滥用风险, 爬虫风险) + 双源受损/滥用增量 + H_history)
M = max(A, P_vendor, D)
```

随后仅当 AbuseIPDB、ProxyCheck / 受损、ipapi.is 滥用三类中至少两类独立证据为正时，按其平均归一化强度计算：

```text
C = min(4.0, 1.0 + 3.0 × 独立证据归一化强度平均值)
```

最后，若爬虫是附加信号而非主信号，额外加入 `min(4.0, 爬虫风险)`：

```text
滥用攻击 = min(40.0, M + C + 可适用的爬虫增量)
```

这种“取最大值 + 有限支持增量”的结构避免了同一恶意历史被多个供应商重复线性扣分。

## 6. 托管基础设施与观测完整性

| 信号 | 风险计算 |
| --- | --- |
| ASN / 组织关键词启发式 | `3.0` |
| ProxyCheck `hosting=true` | `4.0 + 3.0 × R` |
| ipapi.is `is_datacenter=true` | `6.5` |
| 两家均确认托管 / 数据中心 | 额外 `2.5 + 3.0 × R` |
| 基础设施最终值 | `min(12.0, max(上述直接证据) + 双源增量)` |
| `api.ipify.org` 与 `ipwho.is` 同次返回不同出口 IPv4 | `8.0` |
| 国家代码差异、IPv4 / IPv6 国家差异、`egress_service` | `0.0`，仅展示 |

托管、数据中心、企业安全网关、公共 DNS 或受管理出口都可能是合法基础设施；因此该桶不会单独主导总分。ipapi.is 也把受管理出口作为独立字段，APP 仅展示且不把它等同于代理或 VPN。[5]

## 7. 可复核示例

以下情景由仓库中的 `scripts/validate_purity_model.py` 自动验证；脚本还断言类别上限不被突破。

| 原始输入 | 结果摘要 |
| --- | --- |
| AbuseIPDB 30、1 份报告、60 天前 | 滥用攻击风险 8.0，总分 92.0 |
| AbuseIPDB 31、1 份报告、60 天前 | 滥用攻击风险 8.3，总分 91.7 |
| ProxyCheck 风险 40、置信度 40%、30 天前 | 滥用攻击风险 1.8，总分 98.2 |
| ProxyCheck 风险 40、置信度 90%、1 天前 | 滥用攻击风险 4.8，总分 95.2 |
| 单源代理、置信度 40%、30 天前 | 匿名化风险 10.8，总分 89.2 |
| 单源代理、置信度 90%、1 天前 | 匿名化风险 29.0，总分 71.0 |
| Tor Project 官方确认 | 匿名化风险 45.0，总分 55.0 |

这说明模型并非“任何情况都显示一个固定数字”。原始数值变化会传导到一位小数的风险桶和总分；只有底层 API 只给布尔值的证据才保留固定基准，且均受到风险桶上限限制。

## 8. 缺失、隐私与校准治理

缺少 Key、接口超时、配额耗尽、字段为空或来源未覆盖时一律显示“未覆盖”，不扣分。APP 仅按需查询当前公网 IP；不发送账号、Cookie、浏览历史、设备指纹或位置权限。用户自行填入的授权 Key 使用 Android Keystore 的 AES-GCM 密钥在本地加密保存。

所有系数是基于公开字段语义、风险分段和误报控制所设的**可解释先验**，不是基于 MyIPCheck 用户行为训练得出的统计系数。将来只有在合法、最小化和经授权的真实结果数据下，才可以用校准曲线、校准截距和斜率去更新它们。[1]

## References

[1] [Crowson et al., Assessing Calibration of Prognostic Risk Scores](https://pmc.ncbi.nlm.nih.gov/articles/PMC3933449/)

[2] [AbuseIPDB APIv2 Documentation](https://docs.abuseipdb.com/)

[3] [ProxyCheck v3 API Documentation](https://proxycheck.io/api/)

[4] [Tor Project IP API](https://check.torproject.org/api/ip)

[5] [ipapi.is Developers Documentation](https://ipapi.is/developers.html)
