# MyIPCheck 透明纯净度评分模型 v2

**版本：** 2.0  
**状态：** 初始可解释模型；不是任意第三方服务的原始算法，也不是“欺诈概率”。

## 1. 设计目标

新版模型将分数定义为 **0–100 的公开网络出口风险信号指数**：100 表示本次已获取的公开来源中未见风险信号，0 表示多种强风险证据叠加。它不用于判断个人、账号、支付或交易是否欺诈，也不将“未检测到”解释为绝对安全。

> 只有拥有带真实结果标签的业务数据后，才能把分数校准为概率。风险模型的“校准”是预测绝对风险与实际观察风险的匹配；缺少结果标签时，适合输出排序或处置指数，而不应宣称精确概率。[1]

## 2. 评分结构

总风险由四个**互斥或半互斥风险桶**构成。每个桶先在桶内去重并设置上限，再相加；最终指数为 `100 − min(100, 总风险)`。

| 风险桶 | 最高风险 | 目的 | 不重复计分原则 |
| --- | ---: | --- | --- |
| 匿名化网络 | 45 | 识别 Tor、代理与 VPN 出口 | Tor、代理、VPN 同属匿名化；只取最强结论，并只在两家独立数据源确认时给有限交叉验证增量。 |
| 滥用与攻击 | 40 | 汇总 AbuseIPDB、受损、滥用、爬虫和供应商风险分 | 同一恶意行为的不同供应商结果取最高值；独立行为或独立来源确认才小幅增加。 |
| 基础设施 | 12 | 区分托管 / 数据中心与普通出口 | 托管不是恶意行为；启发式关键词最低权重，独立来源确认才提高。 |
| 观测完整性 | 8 | 标识出口观测冲突 | 国家库差异、IPv4/IPv6 地理差异不扣分；只有两个“本机出口 IP”来源不一致时提示。 |

## 3. 权重

### 3.1 匿名化网络桶

| 证据 | 基础风险 | 调整规则 |
| --- | ---: | --- |
| Tor Project 官方接口确认 | 45 | 该桶直接取 45；其他 Tor 标签只作为佐证，不再叠加。 |
| Tor 由其他来源确认，但 Tor Project 未覆盖 | 42 | 多来源 Tor 仍受 45 上限限制。 |
| 代理：单一直接来源 | 32 | ProxyCheck 置信度低于 50 时按 0.45 缩放；50–79 时按 0.70 缩放。 |
| 代理：两个独立来源确认 | 38 | 只比单一来源增加 6，防止多个供应商重复惩罚同一网络事实。 |
| VPN：单一直接来源 | 25 | 同样应用 ProxyCheck 置信度缩放。 |
| VPN：两个独立来源确认 | 30 | 只比单一来源增加 5。 |

Tor 被赋予最高匿名化权重，但并不自动等同于恶意或欺诈；其作用是提示目标服务可能需要额外的访问控制。IPHub、GetIPIntel、IPGeolocation 和 Scamalytics 均将匿名网络与业务风险处理区分开来。[2] [3] [4] [5]

### 3.2 滥用与攻击桶

| 证据 | 风险 | 说明 |
| --- | ---: | --- |
| AbuseIPDB `abuseConfidenceScore` 25–49 / 50–74 / 75–89 / 90–100 | 8 / 18 / 28 / 35 | 使用其公开置信度结果；官方将 75–100 列为拒绝服务时可参考的高风险区间。[6] |
| ProxyCheck 外部风险 25–49 / 50–74 / 75–100 | 5 / 10 / 15 | 这是供应商风险结果的**辅助证据**，不能与 AbuseIPDB 线性相加。 |
| 明确受损（`compromised`） | 20 | 优先于供应商风险分。 |
| 明确滥用（`is_abuser`） | 18 | 与“受损”同时存在时合并为 24，而不是 38。 |
| 明确爬虫（`scraper` / `is_crawler`） | 6 | 单独作为轻量行为提示；已有受损/滥用时最多增加 4。 |
| 两个独立滥用来源同时为正 | +4 | 仅在 AbuseIPDB、ProxyCheck 攻击证据、ipapi.is 滥用三者中至少两个独立类别为正时增加，桶内总分不超过 40。 |

### 3.3 基础设施桶

| 证据 | 风险 |
| --- | ---: |
| ASN / 组织关键词启发式命中 | 3 |
| 一家直接数据源标记托管 / 数据中心 | 7 |
| 两家独立数据源标记托管 / 数据中心 | 12 |
| `egress_service` 受管理出口 | 0，仅说明 |

托管、数据中心、公共 DNS、企业安全网关或隐私中继可能全部是合法基础设施，因此该桶上限严格控制在 12。ipapi.is 也将受管理出口单独标识，不能与代理或 VPN 直接等同。[7]

### 3.4 观测完整性桶

| 证据 | 风险 |
| --- | ---: |
| `api.ipify.org` 与 `ipwho.is` 在同一次诊断中返回不同出口 IP | 8 |
| 国家库不一致、IPv4/IPv6 所在国家不同 | 0，仅显示提示 |

地理库对同一 IP 的国家判断差异、双栈出口跨国、移动网络调度都可能是正常现象，因此不再把地理差异计为纯净度风险。

## 4. 置信度、缺失和回退

当 ProxyCheck 返回 0–100 的置信度时，只有其自身代理 / VPN / 风险类结论会按该值缩放；授权来源和 Tor Project 的明确布尔结论不被错误降低。缺少 Key、接口超时、配额耗尽、字段为空或供应商暂不可用时全部显示为“未覆盖”，绝不扣分。

评分档位为：90–100“低风险信号”、70–89“轻度提示”、40–69“需复核”、0–39“高风险提示”。这是一套独立展示档位，不使用 Ping0、IPQS、Scamalytics、MaxMind 等供应商名称作为结论。

## 5. 校准与版本治理

初始权重来自公开阈值、误报控制和相关信号不重复计分原则；它们是**可解释先验**，不是经过本项目真实欺诈结果训练的统计系数。后续如有在用户授权和合法合规前提下获得的真实业务结果，应按月记录各档位观察到的风险事件率，以校准曲线、校准截距和校准斜率评估并调整阈值。[1] MaxMind 同样建议依据自身的风险分布、欺诈成本和误拒绝成本调优阈值。[8]

## References

[1] [Crowson et al., Assessing Calibration of Prognostic Risk Scores](https://pmc.ncbi.nlm.nih.gov/articles/PMC3933449/)

[2] [IPHub API Documentation](https://iphub.info/api)

[3] [GetIPIntel Detection API](https://getipintel.net/free-proxy-vpn-tor-detection-api/)

[4] [IPGeolocation IP Security API](https://ipgeolocation.io/ip-security-api.html)

[5] [Scamalytics Products](https://scamalytics.com/products/)

[6] [AbuseIPDB API Documentation](https://docs.abuseipdb.com/)

[7] [ipapi.is Developers Documentation](https://ipapi.is/developers.html)

[8] [MaxMind: Set Thresholds for Risk Scores](https://support.maxmind.com/knowledge-base/articles/set-thresholds-for-risk-scores-minfraud)
