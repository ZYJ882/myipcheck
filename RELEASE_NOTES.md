## v1.0.11：分层透明纯净度评分与证据覆盖度

v1.0.11 高于仓库此前发布的 v1.0.10。本次版本将纯净度诊断升级为**公开网络出口风险信号指数**，避免把网络属性、历史行为和数据缺失混合为一个无法解释的“纯净度结论”。

| 更新项 | 说明 |
|---|---|
| 分层风险模型 | 改为直接恶意与滥用（上限 70）、匿名化/透明度（15）、网络上下文（10）和出口可观测性（5）四层风险。 |
| 行为证据优先 | AbuseIPDB、ProxyCheck 的风险、攻击历史、受损与公开滥用标记构成高权重直接行为层；连续分、报告量与时效仍使用连续函数。 |
| 匿名化单列 | Tor、代理和 VPN 被视为当前网络透明度属性，单独展示且严格限权，不再等同于历史恶意。 |
| 基础设施限权 | 云、IDC、托管、ASN 与网段背景仅作为低权重上下文，不可单独主导高风险结论。 |
| 字段缺失治理 | API 数值和布尔字段采用可空解析，明确区分 `false/0`、字段缺失、类型错误和请求失败。未覆盖不扣风险，但会在覆盖度中体现。 |
| 新增覆盖度 | 报告同时显示证据覆盖度、覆盖标签和未覆盖原因；覆盖度低于 50 时优先提示“关键证据覆盖不足”。 |
| 解释文案 | 主分明确为可解释的公开风险信号指数，不是欺诈概率、账号信誉或第三方专有风控值。 |
| 文档与测试 | 新增 `purity_scoring_model_v3.md`，同步模块规格、README 与 v3 场景验证脚本。 |

本版本仍不会读取或上传账号、Cookie、浏览历史、设备指纹或位置权限。用户配置的授权数据源 Key 继续只在本机通过 Android Keystore AES-GCM 加密保存。
