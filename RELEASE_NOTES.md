## MyIPCheck Android

### 本次更新：透明纯净度评分模型 v2.1

本次补丁将 v2 的剩余固定风险分段改为**连续、可逐点复算的小数映射**。诊断仍然只针对当前公网出口的公开网络信号，不读取账号、Cookie、浏览历史或设备指纹。

| 更新项 | 说明 |
| --- | --- |
| 连续 AbuseIPDB 映射 | `abuseConfidenceScore` 的每一点变化都会进入幂函数；报告数量采用对数增量，最近报告日期采用平滑时效衰减。 |
| 连续 ProxyCheck 映射 | `risk` 的每一点变化都会进入幂函数，并由 `confidence` 与 `last_seen` 共同计算可靠度系数；攻击历史数量也采用对数缩放。 |
| 一位小数展示 | 首页总分和各风险桶均显示一位小数，原始数值的细微变化不再被整数取整掩盖。 |
| 透明二元边界 | Tor Project、ipapi.is 等只返回“是 / 否”的字段保留有限的明确事实权重；APP 不会伪造不存在的精确概率。 |
| 防重复与上限 | 同类证据仍在风险桶内去重；匿名化、滥用攻击、基础设施和观测完整性分别保留严格上限。 |
| 逐项解释 | 报告明细会显示 AbuseIPDB / ProxyCheck 的原始分、连续换算结果、攻击历史、来源支持增量与类别上限。 |

评分是独立的**公开网络出口风险信号指数**，不是欺诈概率、个人或账号信誉结论，也不使用任何第三方服务的专有算法。完整公式、示例情景与测试边界见仓库内 [`purity_scoring_model_v2.md`](https://github.com/ZYJ882/myipcheck/blob/main/purity_scoring_model_v2.md)。

### 安装包

本版本由 GitHub Actions 使用固定签名密钥构建。已安装旧版 MyIPCheck 的用户可直接覆盖安装；自动发布流程会递增 `versionCode` 和版本标签。
