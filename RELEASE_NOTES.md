## 本次更新：授权数据源设置入口与自定义 HTTPS 地址

本次更新将“授权数据源 Key”从首页正文移至**顶部地球图标右侧的钥匙图标**。这样首页的网络诊断流程更紧凑，所有敏感配置集中在一个随时可打开的本地设置对话框中。

| 更新项 | 说明 |
| --- | --- |
| 顶部钥匙入口 | 点击顶部地球图标右侧的钥匙图标即可打开授权数据源设置，无需向下滚动到原页面卡片。 |
| 自定义请求地址 | 新增“自定义请求地址（HTTPS）”输入框，可保存完整 HTTPS URL。 |
| 成对配置 | 自定义 API Key 与 HTTPS 请求地址必须同时填写或同时清空，地址必须包含有效 HTTPS 协议与主机名。 |
| 本地加密 | AbuseIPDB Key、ipapi.is Key、自定义地址和自定义 Key 都通过 Android Keystore 的 AES-GCM 密钥加密后仅保存在本机。 |
| 不自动调用 | 自定义地址与 Key 当前仅保存，不会自动发起网络请求、不传出 Key 或当前 IP，也不参与纯净度评分。 |
| 内置数据源 | 保存 AbuseIPDB 或 ipapi.is Key 后，APP 仍会按既有规则重新执行内置纯净度诊断。 |

自定义服务没有统一的认证 Header、HTTP 方法、参数格式或风险字段定义。为避免 APP 猜测接口并意外泄露 Key，本版本只安全保存该配置；后续启用某一自定义服务时，应依据其公开 API 文档明确其请求与解析规则。
