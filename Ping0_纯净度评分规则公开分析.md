# Ping0 纯净度评分规则：公开可验证分析

**分析日期：** 2026-08-27（GMT+8）  
**分析范围：** Ping0 官方公开页面、项目既有调研材料与可访问的公开接口说明。  
**结论性质：** 本文只归纳可公开验证的规则和有限推断；不声称复原 Ping0 未公开的算法、风险数据库、IP 段标签或模型权重。

## 结论摘要

**可以分析出 Ping0 风控值的公开语义、分档阈值和宏观输入维度，但不能从目前公开信息推导出可复算的精确公式。** Ping0 的 IP 风控值是一个方向为“数值越大、风险越高”的 `0–100` 类风险指标。官方明确披露：其基于对扫描、爆破、爬虫、对外攻击、垃圾邮件和 C&C 等危险行为的监测，以及这些行为的**次数与频率**进行打分。[1]

然而，官方并未公开每种行为的权重、观测窗口、时间衰减、来源可信度、同一事件去重、共享 IP 的归因、黑名单融合、人工修正方式或最终聚合函数。因此，不能严谨地写成诸如 `Ping0Risk = 0.4 × spam + 0.3 × crawler + ...` 的公式；任何这种权重表都只能是臆测，不应作为项目的“Ping0 复刻规则”。

| 结论层级 | 可得出的内容 | 不能得出的内容 |
|---|---|---|
| **已证实** | 风控值方向、公开分档、风险行为类别、次数与频率会影响评分 | 具体公式、权重与时间窗口 |
| **合理推断** | 分数依赖持续积累的行为历史和跨时段数据，而非一次性 ASN/定位查询 | 各数据源、标注机制、风险事件如何归因 |
| **不可验证** | 精确扣分表、阈值内部实现、模型版本更新规律、人工调整规则 | 与 Ping0 完全相同的评分结果 |

## 一、官方已披露的规则

### 1. 风控值方向与分档

Ping0 的常见问题页说明：风控值代表 IP 的风险程度，**值越大，风险越高**；该页称 50 以内“安全”，70 以上“可直接拉黑”。官方展示的完整分档如下。[1]

| 风控值 | 官方标签 | 色彩 |
|---:|---|---|
| `≤ 15` | 极度纯净 IP | 深绿色 |
| `15–25` | 纯净 IP | 绿色 |
| `25–40` | 中性 IP | 黄绿色 |
| `40–50` | 轻微风险 IP | 黄色 |
| `50–70` | 稍高风险 IP | 橙色 |
| `> 70` | 极度风险 IP | 红色 |

边界文字存在一个常见的展示歧义：相邻区间共享 15、25、40、50、70 这几个端点。前端通常会采用半开区间，例如 `≤15`、`(15,25]`、`(25,40]` 等；但官方 FAQ 没有披露这些精确端点在代码中的处理方式。对于 MyIPCheck 这样的独立实现，应自行采用无歧义的区间写法，不要把端点行为误称为 Ping0 已公开事实。

### 2. 公开披露的风险行为输入

Ping0 列出的风险行为包括扫描、爆破、爬虫、对外攻击、垃圾邮件及作为木马病毒 C&C 服务。该页进一步明确，**危险行为的发生次数和频率**参与风控打分。[1]

> “Ping0 通过大数据监测 IP 是否有扫描，爆破，爬虫，对外攻击，发送垃圾邮件，作为木马病毒的 C&C 服务等行为，以及危险行为的次数和频率来进行风控打分。” — Ping0 常见问题页 [1]

这能够支持的最大结论是：Ping0 的分数并不是单纯由 IP 是住宅、IDC、代理或某一 ASN 决定，而是包含行为历史维度。FAQ 还明确说明，家庭宽带 IP 也可能因已有上述危险行为而得高风控值；反过来，IDC IP 在没有相关恶意记录时也可能得到较低风控值。[1]

### 3. IP 类型与风控值并非同一规则

项目已有的 Ping0 调研记录了官方 FAQ 关于 IDC / 家宽、原生 IP 和共享人数等信息的说明：这些输出依赖于 IP 段知识、IP 注册与定位信息，以及长期历史数据。即使这些字段会与用户主观感受的“纯净度”相关，官方没有披露它们是否直接进入风控值、以何种方向进入，或分别占多大权重。[3]

因此，以下判断是错误的：**“IDC 必然高风险”“家庭宽带必然低风险”“原生 IP 必然等于低风控值”。** 公开资料仅支持“二者不具有直接一一对应关系”。

## 二、可以推断出的评分模型结构

Ping0 没有公开公式，但根据其公开描述，可以作出非常有限的结构性推断。一个与公开描述一致、但并非官方公式的抽象形态是：

```text
风控值 = G(
  扫描历史,
  爆破历史,
  爬虫历史,
  对外攻击历史,
  垃圾邮件历史,
  C&C 历史,
  各行为的次数、频率与时间分布,
  可能存在的信誉/人工标注/共享使用等长期信息
)
```

其中 `G` 至少应当是历史数据的聚合函数；但无法确认它是线性加权、分段规则、树模型、统计模型、人工标注优先级，还是多种机制的组合。

| 可观察描述 | 可作出的谨慎推断 | 仍然未知 |
|---|---|---|
| “大数据监测” | 数据并非只来自用户单次请求时的即时检测 | 监测网规模、合作数据源与覆盖地区 |
| “危险行为次数和频率” | 评分很可能随事件累积和活跃程度变化 | 计数窗口、频率定义、是否按事件类型分别衰减 |
| 家宽可高、IDC 可低 | IP 类型不构成单一决定性变量 | 类型是否作为弱特征、例外规则或仅独立展示 |
| 70 以上可直接拉黑 | 70 是官方面向使用者给出的处置建议阈值 | 70 是否源自统计校准、经验设定或产品策略 |

尤其不应从“次数和频率”直接假定**线性累加**。真实风险系统通常会处理重复事件、同一来源重复上报、事件严重程度不同、时间久远、污染邻接 IP、NAT/共享出口和误报申诉等问题。Ping0 的公开页面没有说明它是否、以及怎样处理这些问题。

## 三、与 Ping0 环境检测分数的区别

Ping0 还提供一个独立的“环境检测”页面，其当前分数为 **100 分制的环境一致性/异常检测分数**：满分 100，扣分越多表示不一致项、泄漏项或 anti-detect 痕迹越明显。该页面称其覆盖 12 个维度、近 100 项指标和 19 条交叉校验，例如网络出口、WebRTC、DNS、多出口 GeoIP、浏览器 UA 与引擎一致性、时区/语言/字体、硬件指纹及 API 能力等。[2]

这套分数与 IP 查询页的 IP 风控值**方向相反且输入完全不同**：

| 维度 | IP 风控值 | 环境检测当前分数 |
|---|---|---|
| 分高含义 | IP 风险更高 | 环境异常更少、更一致 |
| 公开核心输入 | IP 历史危险行为的次数与频率 | 浏览器、网络、指纹与多维交叉校验 |
| 分数用途 | 反映 IP 公开风险信号 | 反映检测时的环境一致性/泄漏情况 |
| 能否互相替代 | 不能 | 不能 |

将这两个分数混为“Ping0 纯净度分”会得到相反的解释，因此 MyIPCheck 不应把 Ping0 环境检测的扣分逻辑嫁接到 IP 风控值上。

## 四、为什么目前无法精确复刻

Ping0 免费 API 页面仅公开了本机 IP、IPv4/IPv6 以及基础地理、ASN、组织信息的查询方式；没有公开 IP 风控值接口、风险事件明细、权重、训练/规则参数或评分历史。[4]

精确复刻至少需要以下未公开信息：

| 缺失信息 | 对精确复刻的影响 |
|---|---|
| 扫描、爆破、垃圾邮件、C&C 等原始事件数据 | 无法计算行为历史特征 |
| 事件时间戳与观察窗口 | 无法判断新旧事件的影响 |
| 同类事件去重及严重程度 | 无法把“次数”转换为风险贡献 |
| IP 段人工标签、共享人数与历史关系数据 | 无法复现特定网络段的特殊处理 |
| 特征权重、阈值和融合函数 | 无法从输入生成相同的 0–100 分 |
| 数据更新周期与人工修订机制 | 即使某一时刻拟合，也无法保持一致 |

受保护或未公开数据库并不是可以通过页面观察可靠反推出来的信息。对少量 IP 查询结果做“输入—输出拟合”也没有统计识别力：同一个 IP 的隐藏历史特征不可观测，不同 IP 同时改变太多变量，而分数又可能随时间变化。

## 五、对 MyIPCheck 的建议

MyIPCheck 当前采用独立、透明的公开网络出口风险指数，这一定位应保留。它可以借鉴 Ping0 的**展示语言和分档体验**，但不应宣称“Ping0 同款算法”或“复刻 Ping0 风控值”。

建议把两者清晰分层：

| 产品层 | 可采用的做法 | 不应采用的做法 |
|---|---|---|
| 风险语义 | 说明“分越高表示当前公开风险信号越多” | 暗示是欺诈概率、账号风险或绝对安全性 |
| 阈值展示 | 可设计自有的低/中/高风险档位，并明确为自有标准 | 直接复制 Ping0 分档后声称官方兼容 |
| 数据实现 | 使用 AbuseIPDB、ProxyCheck、Tor、ipapi.is 等公开可审计信号 | 假装拥有 Ping0 的行为数据库、共享人数或人工 IP 段库 |
| 解释能力 | 展示来源、字段、时效、权重和风险桶 | 输出不可解释的“纯净度结论” |

如果目标只是让用户对比阅读，MyIPCheck 可以新增一个**“Ping0 公开分档参照”**卡片，内容仅为“≤15 极度纯净、15–25 纯净……>70 极度风险”，同时加注“该分档属于 Ping0 对其自有风控值的解释；本应用的独立风险指数不与其数值等价”。这样既满足用户认知，也避免算法和品牌上的误导。

## 最终判断

Ping0 的公开评分规则可归纳为：**以 IP 历史危险行为及其次数、频率为核心输入，输出风险越高数值越大的 0–100 风控值，并使用六档阈值解释风险程度。** 这已经足以解释分数的方向、宏观含义和官方建议阈值。

但其**具体权重、时间逻辑、数据源、去重、人工标注与聚合公式未公开**。所以正确结论不是“已分析出 Ping0 的完整评分算法”，而是“已分析出 Ping0 公开披露的评分框架和分档，精确算法不可由公开资料可靠还原”。

## References

[1]: [Ping0 常见问题说明：风控值定义、分档及危险行为依据](https://ping0.cc/ip/faq)

[2]: [Ping0 环境检测：当前分数含义、检测维度及数据收集说明](https://ping0.cc/env)

[3]: [MyIPCheck 项目内调研：Ping0 纯净度识别与 Android 实现边界](https://github.com/ZYJ882/myipcheck/blob/e5a0fb0f29ce91b232604da91005e592fa4ac31d/docs_ping0_purity_research.md)

[4]: [Ping0 免费 API 接口说明](https://ping0.cc/ip/api)
