# 增强透明纯净度诊断模块规格 v2

## 产品定位

“增强透明纯净度诊断”是 MyIPCheck 的**独立、可解释公开网络出口风险信号指数**。它参考公开 IP 风险服务对风险分段、行动阈值与误报控制的说明，但不复制、反推或声称使用任何厂商的专有算法、人工 IP 段标注、服务器或商业风险数据库。[1] [2] [3]

> **100 分**仅表示本次已经成功覆盖的公开来源中未见风险信号；它不是“绝对安全”、欺诈概率、账号信誉、个人评价或业务决策结论。缺少真实结果标签时，分数只能作为可解释的排序 / 提示指数，不能被包装成校准后的概率。[4]

## 评分计算

初始分数为 100。APP 先将原始证据归入四个风险桶；每个桶先执行同类去重和上限限制，之后合计风险，最后计算 `总分 = 100 − min(100, 四类风险之和)`。网络超时、未知字段、未配置 Key、配额失败或未覆盖来源永不扣分。

| 风险桶 | 上限 | 可计入的证据 | 去重与误报控制 |
| --- | ---: | --- | --- |
| 匿名化网络 | 45 | Tor、代理、VPN | Tor / 代理 / VPN 视为同类网络属性，仅取最强结论；两个独立来源确认时只增加有限交叉验证分。 |
| 滥用与攻击 | 40 | AbuseIPDB 置信度、ProxyCheck 风险、受损、滥用、爬虫 | 同一风险类别跨来源取最高结果；独立类别同时命中时小幅加分，总分不超过 40。 |
| 托管基础设施 | 12 | ASN / 组织启发式、托管、数据中心 | 托管不是恶意；启发式最低权重，直接来源和双来源确认才提高。 |
| 观测完整性 | 8 | 同次检测的公网出口 IP 冲突 | 国家代码和 IPv4/IPv6 地理差异仅展示，不参与风险。 |

### 匿名化网络桶

| 已确认信号 | 风险 | 规则 |
| --- | ---: | --- |
| Tor Project 官方确认 | 45 | 直接使用该桶上限；其他 Tor 标记仅作佐证。 |
| 其他来源 Tor 标记 | 42 | 官方接口不可用时使用，桶内上限仍为 45。 |
| 单源 / 双源代理 | 32 / 38 | ProxyCheck 置信度低于 50 时乘 0.45、50–79 时乘 0.70。 |
| 单源 / 双源 VPN | 25 / 30 | 规则与代理相同。 |

### 滥用与攻击桶

| 已确认信号 | 风险 |
| --- | ---: |
| AbuseIPDB `abuseConfidenceScore` 25–49 / 50–74 / 75–89 / 90–100 | 8 / 18 / 28 / 35 |
| ProxyCheck 风险分 25–49 / 50–74 / 75–100 | 5 / 10 / 15 |
| 明确受损 | 20 |
| 明确滥用 | 18 |
| 同时明确受损与滥用 | 24，不线性相加 |
| 明确爬虫 | 6；已有更强滥用证据时最多额外 4 |
| 至少两类独立滥用证据 | +4；桶内最高 40 |

AbuseIPDB 的置信度区间用于把公开结果转换为本模块的输入分段，不等同于本模块声称的欺诈概率；其高分仅表示较强的公开滥用报告证据。[5]

### 托管基础设施与观测完整性

| 已确认信号 | 风险 |
| --- | ---: |
| ASN / 组织关键词启发式命中 | 3 |
| 一家直接来源标记托管 / 数据中心 | 7 |
| 两家独立来源标记托管 / 数据中心 | 12 |
| `egress_service` 受管理出口 | 0，仅展示 |
| `api.ipify.org` 与 `ipwho.is` 返回不同出口 IP | 8 |
| 国家代码差异、IPv4 / IPv6 国家差异 | 0，仅展示 |

## 评分档位和展示

| 分数 | 标签 | 说明 |
| ---: | --- | --- |
| 90–100 | 低风险信号 | 已覆盖来源未见明显风险信号；未覆盖不是安全结论。 |
| 70–89 | 轻度提示 | 存在有限、可解释的网络属性或行为提示。 |
| 40–69 | 需复核 | 发现较强匿名化、滥用或观测冲突信号。 |
| 0–39 | 高风险提示 | 多个独立风险桶同时命中；不可单独据此推断个人、账号或交易。 |

报告先显示四个风险桶的“实际风险 / 上限”，再展示来源明细、数据缺失状态和每项处理逻辑。Android VPN 与 Private DNS 仅展示系统状态，不参与纯净度计分。

## 数据、密钥与隐私

诊断会按需调用 `api.ipify.org`、`ipapi.co`、`ipwho.is`、`proxycheck.io` 和 Tor Project 接口。用户自行填写 Key 后，APP 还会调用 AbuseIPDB APIv2 Check 与 ipapi.is 官方 JSON POST 端点。[5] [6] [7]

调用只使用当前公网 IP 或由第三方服务自动观察到的请求来源 IP；APP 不传递账号、Cookie、浏览历史、设备指纹、位置权限或其他个人敏感信息。AbuseIPDB、ipapi.is 和自定义预留 Key 通过 Android Keystore 的 AES-GCM 密钥加密后保存在应用私有偏好设置中，并禁用系统备份与设备迁移；预留 Key 当前不会发送或参与评分。检测结果仅保留在当前内存界面，APP 不保存 IP 历史或检测报告。第三方服务仍可能依据各自隐私政策记录请求日志。

## 版本治理与校准边界

权重是基于公开分段、供应商概率解释和误报控制原则构建的**可解释先验**。它不是基于本项目用户行为训练的机器学习模型。只有在获得合法、最小化、经授权的真实结果数据后，才能按风险档位观察事件率、检查校准曲线并安全调整阈值。[4] 在此之前，任何发布版本都不得将该分数描述为真实欺诈概率。

详细权重、示例情景和参考资料见仓库根目录 [`purity_scoring_model_v2.md`](purity_scoring_model_v2.md)。

## 参考

[1] [GetIPIntel Detection API](https://getipintel.net/free-proxy-vpn-tor-detection-api/)

[2] [Scamalytics Products](https://scamalytics.com/products/)

[3] [IPGeolocation IP Security API](https://ipgeolocation.io/ip-security-api.html)

[4] [Crowson et al., Assessing Calibration of Prognostic Risk Scores](https://pmc.ncbi.nlm.nih.gov/articles/PMC3933449/)

[5] [AbuseIPDB API Documentation](https://docs.abuseipdb.com/)

[6] [ipapi.is Developers Documentation](https://ipapi.is/developers.html)

[7] [Tor Project IP API](https://check.torproject.org/api/ip)
