# MyIPCheck 透明网络出口风险模型 v3.0

**状态：** 可解释的公开网络出口风险信号指数；不是欺诈概率、个人/账号信誉，也不是任意第三方服务的专有风控算法。  
**实现位置：** `android/app/src/main/java/ing/ipcheck/netscope/MainActivity.kt` 的 `runPurityDiagnosis()`。  
**验证脚本：** `scripts/validate_purity_model.py`。

## 1. 输出与边界

v3 将旧版单一“纯净度”解释为一个由已识别风险反推的主分数，并**同时**展示证据覆盖度。主分越高，仅表示本次成功覆盖的公开来源中可计入的风险信号越少；当关键来源或字段未覆盖时，覆盖度会下降，主分不能被解释为“安全保证”。

```text
总风险 = clamp(直接恶意与滥用 + 匿名化/透明度 + 网络上下文 + 出口可观测性, 0, 100)
主分   = 100 - 总风险

覆盖度 = Σ(风险层权重 × 该风险层是否获得有效字段) / 100 × 100
```

| 风险层 | 风险上限 / 覆盖权重 | 语义 | 误报控制 |
|---|---:|---|---|
| 直接恶意与滥用 | 70 | 近期、可溯源的公开滥用、攻击、受损与爬虫信号 | 同类供应商不线性相加；多来源仅给予有限交叉支持。 |
| 匿名化 / 透明度 | 15 | Tor、代理、VPN 等当前网络属性 | 单列展示，不等同于历史恶意。 |
| 网络上下文 | 10 | ASN、云/IDC/托管等背景 | 只作低上限先验，不以基础设施身份推断恶意。 |
| 出口可观测性 | 5 | 同次出口 IPv4 不一致 | 只反映观测路径差异，不推断历史行为。 |

> 只有拥有合法、最小化、定义明确的真实事件标签，并在独立的时间外样本上完成校准验证后，才可以将分数命名为风险概率。当前版本始终是**风险信号指数**。

## 2. 时效与 ProxyCheck 可靠度

当来源返回最近报告/检出时间时，距今 `d` 天的时效系数为：

```text
F(d) = clamp(0.45 + 0.55 × e^(-d / 21), 0.45, 1.00)
```

ProxyCheck 的 0–100 `confidence` 及最近检出时间只用于缩放 ProxyCheck 自身信号：

```text
R = clamp((0.30 + 0.70 × confidence / 100) × F(last_seen), 0.25, 1.00)
```

`R` 是该来源信号的缩放可靠度，不是 IP 为代理、恶意或欺诈的概率。时间缺失时，ProxyCheck 使用 `0.70` 回退；AbuseIPDB 的最近报告时效使用 `0.60` 回退。

## 3. 直接恶意与滥用层（上限 70）

AbuseIPDB 的可用原始字段会被保留为连续输入：

```text
A_base    = 34 × (abuseConfidenceScore / 100)^1.35
A_reports = 2 × ln(1 + totalReports) / ln(101)
A_fresh   = 2 × F(lastReportedAt)        （仅当 abuseConfidenceScore > 0）
A          = min(36, A_base + A_reports + A_fresh)
```

ProxyCheck 风险、攻击标记及攻击历史的输入为：

```text
P_vendor  = 16 × (risk / 100)^1.20 × R
H_history = 4 × ln(1 + attackEvents) / ln(101) × R

compromised = 12 + 8 × R                 （仅字段明确为 true）
abuser      = 16                          （仅字段明确为 true）
crawler     = 2 + 4 × R 或 4              （仅字段明确为 true）
D           = min(28, max(compromised, abuser, crawler) + 双源增量 + H_history)
```

先将三种主要直接证据标准化，再以饱和式聚合，避免多个高度相关的供应商结论被线性重复扣分：

```text
primary = clamp(max(A / 36, P_vendor / 16, D / 28), 0, 1)

corroboration = 0                            （独立证据少于 2 类）
              = min(0.20, 0.08 + 0.12 × 独立证据平均强度)

crawlerSupport = min(0.08, crawler / 50)     （爬虫为附加而非主证据时）

行为风险 = clamp(70 × [1 - (1-primary) × (1-corroboration) × (1-crawlerSupport)], 0, 70)
```

供应商“不同”不自动等于统计学独立；`corroboration` 仅表示不同供应商或字段语义的有限交叉支持，不应理解为独立事件概率。

## 4. 匿名化 / 透明度层（上限 15）

| 证据 | 风险计算 |
|---|---:|
| Tor Project 官方确认 | `15.0` |
| 其他来源 Tor，且 ProxyCheck 也确认 | `13.0 + 1.5 × R` |
| 其他来源 Tor | `13.0` |
| 仅 ProxyCheck 代理 | `10.0 × R` |
| 仅 ipapi.is 代理 | `9.0` |
| 双源代理 | `max(9.0, 10.0 × R) + 2.0 × R` |
| 仅 ProxyCheck VPN | `8.0 × R` |
| 仅 ipapi.is VPN | `7.0` |
| 双源 VPN | `max(7.0, 8.0 × R) + 1.5 × R` |

Tor、代理和 VPN 是同一类网络透明度事实，最终只取适用结论中的最大值并封顶，不彼此累加。该层不能作为“历史滥用已证实”的证据使用。

## 5. 网络上下文与出口可观测性

| 信号 | 风险计算 | 解释 |
|---|---:|---|
| ASN / 组织关键词启发式 | 2.0 | 低权重背景先验。 |
| ProxyCheck `hosting=true` | `2 + 2 × R` | 当前来源的托管属性。 |
| ipapi.is `is_datacenter=true` | 4.5 | 当前来源的数据中心属性。 |
| 双源托管 / 数据中心 | 额外 `1.5 + 2 × R` | 有限交叉支持。 |
| 网络上下文最终值 | `min(10, max(直接上下文) + 双源增量)` | 云/IDC/托管不等于恶意。 |
| ipify 与 ipwho.is 同次 IPv4 不一致 | 5.0 | 只表示出口可观测异常。 |

国家代码差异、IPv4/IPv6 地理差异、受管理出口、Android VPN 状态及 Private DNS 状态仅展示，不计入历史滥用风险。

## 6. 覆盖度、缺失与展示规则

每个风险层是否“覆盖”由**有效字段**决定，而不是只看 HTTP 请求是否成功。字段缺失、格式错误、来源响应不完整、请求超时、Key 未配置与配额耗尽都不会扣风险，但会降低覆盖度；它们不能显示为“未检出”。

| 覆盖度 | 标签 | 主分解释 |
|---:|---|---|
| 80–100 | 证据覆盖较完整 | 可以作为本次公开风险信号的较可靠概览。 |
| 50–79 | 部分覆盖 | 可能遗漏部分直接行为或网络属性信号。 |
| 0–49 | 覆盖不足 | 主分只反映已覆盖来源，不能据此判断出口安全。 |

展示档位仍以总风险反推的主分为基础；当覆盖度低于 50 时，无论主分多高，报告优先显示“关键证据覆盖不足”。

| 主分 | 标签 |
|---:|---|
| 90–100（且覆盖度至少 50） | 低风险信号 |
| 70–89.9 | 轻度提示 |
| 40–69.9 | 需复核 |
| 0–39.9 | 高风险提示 |

## 7. 数据来源与治理边界

当前实现按需查询当前公网 IP 或由服务观察到的请求来源 IP；不会传递账号、Cookie、浏览历史、设备指纹或位置权限。外部来源包括 ProxyCheck、Tor Project，以及在用户本地配置 Key 后启用的 AbuseIPDB 与 ipapi.is。用户 Key 仍使用 Android Keystore AES-GCM 加密并仅保存在本机。

模型权重是公开字段语义、连续映射和误报控制所形成的可解释先验。后续若获得经授权、最小化且独立于供应商风险结论的真实标签，应使用时间外验证、校准曲线、校准截距/斜率、PR-AUC、误报率和漏报率进行评估，并以版本化方式更新参数。
