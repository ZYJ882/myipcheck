# 科学且实用的 IP 纯净度评分规则建议

**适用对象：** MyIPCheck 的“当前公网出口风险/纯净度”功能  
**分析日期：** 2026-08-27（GMT+8）  
**核心结论：** 不存在脱离用途、标签数据和误报成本的“全网唯一最佳”IP 评分公式。对 MyIPCheck 而言，当前阶段最科学、最好用的设计不是追求一个伪精确的 0–100“纯净概率”，而是采用**双层输出**：一层是可解释、可复算的“公开风险信号指数”，另一层是独立的“覆盖度/证据强度”。当积累到经过授权、可审计的真实结果标签后，才升级为经时间外验证与概率校准的滥用风险概率。

## 1. 什么才算“科学、好用”

对 IP 评分而言，科学性不是把更多供应商分数相加，也不是分数看起来足够精细；它至少要求评分目标可定义、输入可追溯、时间逻辑合理、相互依赖的证据不会重复计分、输出能接受数据验证，并能随着威胁环境变化重新评估。NIST 的风险管理框架把风险测量、性能评估、持续监测、透明度与模型更新列为可信系统的重要原则。[1]

“最好用”还取决于用途。**网络出口风险提示**、**反垃圾邮件处置**、**登录防护**和**账号业务兼容性**并不是同一个预测问题，不能共用一个未定义含义的“纯净度百分比”。Spamhaus 也将 IP 信誉描述为对 IP 或 IP 网段可信度的评估，强调服务商、IP 邻域、关联基础设施、使用时间、首次出现时间和使用方式等多维上下文，而不是某一单一字段。[2]

| 评价维度 | 科学要求 | MyIPCheck 的实际含义 |
|---|---|---|
| **目标有效性** | 定义待预测结果与时间窗口 | 例如“未来 30 天内是否出现已确认网络滥用事件” |
| **可解释性** | 能说明每项风险来自什么来源、何时发生、如何折算 | 展示事件类别、时效、来源覆盖与组内聚合 |
| **校准性** | 预测 20% 的样本应约有 20% 实际命中事件 | 只有拥有真实标签后才能声称“概率” |
| **区分能力** | 高风险样本应比低风险样本更常命中事件 | 以时间外测试集评估 PR-AUC、召回率和误报率 |
| **鲁棒性** | 供应商缺失、字段变化或攻击模式变化不应导致虚高“安全”分 | 评分和覆盖度分开，缺失必须可见 |
| **公平与误报控制** | 云厂商、国家、ASN 或 VPN 身份不能被直接等同于恶意 | 上下文特征仅作低上限先验，直接行为证据优先 |

> **关键定义：** 没有真实结果标签时，0–100 只能是“风险信号指数”或“排序分”；只有在明确的目标事件、时间窗口和独立校准验证存在时，才可把分数解释为“发生该事件的概率”。良好校准的概率模型应满足：模型预测约 0.8 的样本中，约 80% 实际为正例。[3]

## 2. 先把“一个分数”拆为三个不同概念

现有 IP 检测产品很容易把“是否代理”“是否云厂商”“是否有攻击历史”“数据是否覆盖”全部扣进一个纯净度分。但这些信号的语义不同，混合后会使分数既不够科学，也不够好用。

建议应用同时展示以下三个维度；其中第一个作为默认主指标，后两项作为必要上下文。

| 输出 | 建议范围与方向 | 应包含什么 | 不应被误解为 |
|---|---|---|---|
| **公开滥用风险指数** | 0–100；越高表示公开直接/间接风险信号越多 | 已知攻击、恶意活动报告、受损标记、近期性、来源支持 | 个人可信度、账号风险、真实欺诈概率 |
| **网络透明度 / 匿名化状态** | 0–100；越高表示 Tor、代理、VPN、出口不一致等更明显 | Tor、代理、VPN、HTTP/WebRTC/多出口差异 | 恶意行为的直接证据 |
| **证据覆盖度** | 0–100；越高表示更多关键数据源和字段成功覆盖 | 数据源可用性、字段完整性、时效、相互验证程度 | “IP 更安全” |

这一拆分解决两个典型误导。第一，**Tor/VPN 是网络属性，不是已证实滥用行为**；它可以影响某些业务的访问策略，却不应等价于“IP 曾经攻击”。第二，数据源未覆盖时不能给出虚高的 100 分；正确做法是保留风险指数的已知结果，同时降低或标记覆盖度，而不是把未知当作无风险。

## 3. 推荐的 v3 规则：无自有标签时的透明风险信号指数

在没有自有真实标签数据的阶段，不应假装训练机器学习概率模型。更好的方案是采用**事件家族分组、时效衰减、来源可靠度、组内去重、类别上限**的透明规则。这种规则仍是先验，但每一步都可解释、审计与日后替换。

### 3.1 输入应以“证据”而不是“供应商总分”为中心

建议把所有返回数据转化为带来源、时间与语义的证据记录，而不是只存一个供应商风险值。

| 证据类别 | 示例输入 | 推荐定位 | 初始风险预算 |
|---|---|---|---:|
| **直接恶意行为** | C&C、恶意软件分发、已确认攻击、凭据攻击、漏洞利用 | 最强证据；按类型、次数、时效计分 | 0–45 |
| **可疑/滥用行为** | 扫描、爆破、爬虫、垃圾邮件、公开滥用报告 | 中强证据；需要来源与时效控制 | 0–30 |
| **匿名化网络属性** | Tor、代理、VPN、匿名出口 | 业务兼容性或风险提示；非直接恶意 | 0–15 |
| **网络上下文先验** | /24 邻域、ASN、主机商、数据中心、首次出现时间 | 冷启动和背景信息；不能主导结论 | 0–10 |
| **出口可观测异常** | HTTP/多出口/WebRTC 地址冲突 | 诊断/透明度证据；不应推断历史恶意 | 0–5 |
| **总风险上限** | 以上各类求和并截断 | 避免单一属性主导 | 100 |

这比当前 MyIPCheck 的 `匿名化 45 / 滥用攻击 40 / 基础设施 12 / 完整性 8` 更符合“**直接行为优先、匿名化与基础设施次之**”的科学语义。若产品另有“账号环境兼容性”用途，应使用独立的环境一致性分，而不应提高 IP 滥用风险中的 Tor/VPN 权重。

### 3.2 直接行为证据的可复算公式

对每一个**行为家族** `g`（例如扫描、爆破、垃圾邮件、C&C），将来自某个来源 `s` 的一条或一组证据转换为 0–1 的标准化强度：

```text
x(g,s) = severity(g)
       × sourceReliability(s)
       × volume(count; τg)
       × recency(age; h_g)

volume(n; τg) = 1 - exp(-n / τg)
recency(d; h_g) = 2^(-d / h_g)
```

其中，`severity(g)` 表示行为类型的相对严重性；`sourceReliability(s)` 表示来源质量、误报处理、字段完整性和样本覆盖的预先设定或经校准的可靠度；`volume` 使用饱和函数防止重复报告线性放大；`recency` 使用半衰期 `h_g` 让旧事件平滑衰减，而非在第 7、30 或 90 天突然跳变。

同一行为家族的多个来源应采用**最大值或相关性折减后的聚合**，不应直接相加：

```text
E_g = max_s x(g,s)
```

然后再对不同的行为家族采用递减增益合并：

```text
B = 75 × [1 - Π_g (1 - min(E_g, 0.95))]
```

`B` 是直接行为和可疑/滥用行为的组合风险预算。其优点是：第一个强证据贡献最大；第二个不同家族的证据仍会提高风险；但同一事件被多个供应商重复观察时不会被重复线性扣分。

| 参数 | 首发建议 | 正确使用方式 |
|---|---|---|
| `severity(g)` | C&C/已确认恶意最高；漏洞利用、凭据攻击次之；扫描/爬虫更低 | 作为明确、可审计的产品先验；后续由标签数据学习 |
| `τg` | 使少量报告快速产生提示、更多报告边际递减 | 按行为类型设定并以误报率回测，而非所有类型共用 |
| `h_g` | 短期爆破/扫描使用较短半衰期；C&C/恶意软件基础设施可更长 | 以真实事件“再出现率”或业务损失曲线估计 |
| `sourceReliability(s)` | 由来源透明度、字段粒度、历史精确率和覆盖度决定 | 不能把多个供应商默认视为统计独立 |

AbuseIPDB 的 API 公开提供 `abuseConfidenceScore`、所选时间窗口内的 `totalReports` 以及 `lastReportedAt`；其文档也明确将置信分描述为非二元的滥用评估。因此，将分数、报告数量与最近时间分开处理，比映射为固定扣分更具信息保真性。[4]

### 3.3 Tor、代理、VPN 与托管网络的处理

匿名化网络和基础设施属性的最佳实践是**单列、限权、可解释**。将其视为直接恶意行为会系统性误伤企业网关、远程办公、云服务、公共 DNS、隐私保护工具及共享 NAT 出口。

推荐规则如下：

| 属性 | 建议处理 | 理由 |
|---|---|---|
| Tor 官方出口 | 透明度风险可高；滥用风险仅给予有限条件性增量 | 强匿名属性明确，但本身不证明攻击历史 |
| 代理 / VPN | 透明度风险中等；滥用风险不因属性本身直接叠加 | 代理/VPN 可能是合法企业或隐私工具 |
| 云/IDC/托管 ASN | 上下文先验，风险上限低 | 基础设施身份不等于行为风险 |
| 同一 /24 高风险密度 | 低到中等先验；需向单 IP 直接行为让位 | 对新 IP 冷启动有用，但容易造成“邻居污染” |
| HTTP、WebRTC、多出口冲突 | 透明度/环境异常项；最多轻微风险 | 说明当前路径差异，不证明历史恶意 |

CrowdSec 的公开 IP 网段信誉机制将 /24 内被报告过的 IP 数、进入其情报拦截列表的数量、IP 留存时长、报告引擎可信度和行为类型作为额外证据，而非把网段统计完全替代单个 IP 证据。[5] DAbR 研究也展示了如何把 ASN、ISP、国家、用户类型和位置等属性建模为与已知恶意样本的相似度，但该方法天然会把结构性属性映射为风险。[6] 因此，在 MyIPCheck 中这些变量只应是有明确上限的**背景先验**，绝不能主导“高风险”判定。

### 3.4 覆盖度必须独立计算

缺失数据不应扣成风险，也不能悄悄变成“安全”。建议计算独立覆盖度：

```text
Coverage = 100 × (Σj importance(j) × complete(j)) / Σj importance(j)
```

`complete(j)` 取 `0` 或 `1`，或按字段完整度取 0–1；例如“当前 IP 已得到”“来源响应成功但核心字段缺失”“接口超时”“配额耗尽”必须是不同状态。用户界面应同时显示：

| 覆盖度 | 建议显示 | 分数解释限制 |
|---:|---|---|
| `≥ 80` | 证据覆盖较完整 | 可作为当前公开信号的较可靠概览 |
| `50–79` | 部分覆盖 | 提醒可能遗漏部分风险或正面信号 |
| `< 50` | 覆盖不足 | 不显示“高纯净”；应显示“未充分评估” |

## 4. 真正科学的终局：有标签后使用校准概率模型

如果 MyIPCheck 能合法、最小化、经过授权地获得真实的结果标签，例如“某 IP 在未来 30 天内是否被本方或可信合作方确认发生某类滥用”，就可以从“信号指数”升级为预测模型。

### 4.1 先定义标签，再选算法

正确的标签不是“供应商说它高风险”，因为那会让模型学会复制供应商；应是独立的、可审计的结果，例如：

```text
Y = 1：在评分时点后 30 天内，该出口 IP 被确认发生目标类别的滥用事件
Y = 0：在相同观察期内未发生目标类别的已确认事件
```

必须把目标类别（例如垃圾邮件、扫描、登录攻击或所有网络滥用）、观察期和数据采集边界写入版本化文档。不同业务应使用不同标签；“邮件信誉”模型不能直接移植为“账户登录风险”模型。

### 4.2 建模路线

建议采用“可解释基线 + 性能挑战者 + 概率校准”路线。

| 阶段 | 推荐方法 | 产物 |
|---|---|---|
| 基线 | 正则化 Logistic Regression 或带单调约束的 GAM | 可解释系数、特征方向、稳定的基准 |
| 挑战者 | 单调约束梯度提升树 | 对非线性、特征交互的性能比较 |
| 概率校准 | Platt scaling、isotonic regression 或 beta calibration | 将原始模型分数转换为可解释概率 |
| 不确定性 | 覆盖度、样本密度、置信区间或保守区间 | 避免对冷门网段和缺失信号过度自信 |

最终形式为：

```text
p_raw = Model(直接行为特征, 时效特征, 来源质量, 上下文先验, 覆盖状态)
p_cal = Calibrate(p_raw, 时间外验证集)
风险概率 = 100 × p_cal
```

只有在校准通过后，才可以说“风险概率 20%”。scikit-learn 的概率校准文档说明，校准曲线会按预测分箱比较模型平均预测概率与实际正例比例；这正是验证“20% 是否真的约等于 20%”的基本工具。[3]

### 4.3 验证不能随机切分，必须按时间切分

IP 滥用数据具有强烈的时间性、重复出现与数据漂移。把同一时期的随机样本分到训练集和测试集，会让模型利用未来信息或近重复样本，得到虚高结果。推荐做法是：

| 验证层 | 做法 | 目的 |
|---|---|---|
| **时间外测试** | 早期时间段训练，后续完整时间段测试 | 检验对未来威胁环境的泛化能力 |
| **网段/ASN 分组测试** | 同一网段或 ASN 尽量不同时出现在训练与测试 | 限制记住网络邻域而非学习行为模式 |
| **地区/运营商切片** | 按国家、ASN 类型、住宅/IDC、IPv4/IPv6 分层 | 排查结构性误伤 |
| **缺失模式切片** | 分别检查数据源完整与不完整的样本 | 防止缺失被当作安全 |
| **滚动回测** | 定期移动训练窗口和测试窗口 | 检验漂移并决定更新频率 |

评估至少应同时报告排序、概率和业务处置三类指标：

| 指标 | 回答的问题 |
|---|---|
| PR-AUC、Precision@K、Recall@K | 在罕见滥用事件中，高分队列是否真的更集中命中？ |
| ROC-AUC | 整体排序是否有效？不能单独作为结论 |
| 校准曲线、校准截距/斜率、Brier score | 预测百分比是否接近实际发生率？ |
| 不同阈值下的误报率、漏报率、拦截量 | 阈值会对真实用户和安全运营造成什么影响？ |
| 延迟、覆盖度、来源失效比例 | 生产环境是否仍能稳定工作？ |

## 5. 对现有 MyIPCheck v2.1 的具体调整建议

当前 v2.1 的优点是连续映射、时间衰减、风险桶上限和跨来源有限增量；这些是良好起点。建议下一版本按以下顺序改进。

| 优先级 | 调整 | 预期收益 |
|---|---|---|
| **P0** | 把“字段缺失”“明确 false/0”“请求失败”编码为不同状态，并将覆盖度独立展示 | 避免把未知误说成干净 |
| **P0** | 将“纯净度”主名改为“公开网络出口风险信号”，并禁止概率性文案 | 消除分数含义误导 |
| **P1** | 将匿名化/代理/VPN从滥用风险中拆出为独立透明度维度，降低其对历史恶意结论的影响 | 减少对合法隐私工具和企业网络的误伤 |
| **P1** | 以“行为家族”为单位去重；多供应商相同语义只取最强或作有限支持增量 | 控制相关来源的重复扣分 |
| **P1** | ASN、IDC、/24 邻域只作为低权重先验，并设置严格上限 | 防止网络身份替代行为证据 |
| **P2** | 记录匿名化的特征快照、版本、来源、时间和评分分解 | 支持回放、申诉、回归测试与模型更新 |
| **P2** | 获得合法标签后建立时间外验证、校准报告与阈值成本表 | 从启发式指数升级为科学概率模型 |

## 6. 最终推荐

对当前 MyIPCheck，建议采用下面这条产品原则：

> **用直接、近期、可溯源的恶意行为证据主导公开风险；把 Tor/VPN、云厂商、网段和出口异常作为单列且受限的上下文；把覆盖度独立展示；在没有标签校准前，绝不把 0–100 分称为概率。**

这比模仿 Ping0 的固定区间，或把多家供应商的“风险分”直接相加，更符合公开研究和成熟信誉服务的共同方向：多源、时间敏感、上下文丰富、持续更新、可解释，并对误报保持审慎。[2][4][5][6]

## References

[1]: [NIST — AI Risk Management Framework](https://www.nist.gov/itl/ai-risk-management-framework)

[2]: [Spamhaus — IP Address Reputation](https://www.spamhaus.org/ip-reputation/)

[3]: [scikit-learn — Probability Calibration](https://scikit-learn.org/stable/modules/calibration.html)

[4]: [AbuseIPDB — APIv2 Documentation](https://docs.abuseipdb.com/)

[5]: [CrowdSec — Introducing the IP Range Reputation System](https://www.crowdsec.net/blog/introducing-the-ip-range-reputation-system)

[6]: [Renjan et al. — DAbR: Dynamic Attribute-based Reputation Scoring for Malicious IP Address Detection (2018)](https://ebiquity.umbc.edu/_file_directory_/papers/902.pdf)

[7]: [Porenta & Ciglarič — Empirical Comparison of IP Reputation Databases (CEAS 2011)](https://doi.org/10.1145/2030376.2030402)
